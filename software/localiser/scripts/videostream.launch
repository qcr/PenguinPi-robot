#!/bin/bash
SHUTTER_SPEED=$(cat shutterspeed)
TIMEOUT_MS=20000
BITRATE=100000
raspivid -fl -t 0 -w 640 -h 480 -fps 25 -l -o - | nc -k -l 5000 &
echo $! > /var/www/penguinpi/videostream.pid
echo "Started video stream"
